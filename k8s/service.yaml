apiVersion: v1
kind: Service
metadata:
  name: aishwarya-webpage-service
  labels:
    app: aishwarya-webpage
spec:
  selector:
    app: aishwarya-webpage
  type: NodePort
  ports:
    - protocol:   TCP
      port:       80
      targetPort: 80
      nodePort:   30080
